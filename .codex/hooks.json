{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python -X utf8 -c \"import pathlib, runpy, subprocess; root = subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], encoding='utf-8').strip(); runpy.run_path(str(pathlib.Path(root) / '.codex/hooks/session_start.py'), run_name='__main__')\"",
            "statusMessage": "Loading project guardrails"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python -X utf8 -c \"import pathlib, runpy, subprocess; root = subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], encoding='utf-8').strip(); runpy.run_path(str(pathlib.Path(root) / '.codex/hooks/pre_tool_use.py'), run_name='__main__')\"",
            "statusMessage": "Checking project tool policy"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python -X utf8 -c \"import pathlib, runpy, subprocess; root = subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], encoding='utf-8').strip(); runpy.run_path(str(pathlib.Path(root) / '.codex/hooks/user_prompt_submit.py'), run_name='__main__')\"",
            "statusMessage": "Detecting prompt mode"
          }
        ]
      }
    ]
  }
}
